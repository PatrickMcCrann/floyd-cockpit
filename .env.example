# Required only for the AI-written memo on Screen 3.
# Screens 1 and 2 (the model and the integrity check) run fully without it.
# Copy to .env and fill in:  cp .env.example .env
ANTHROPIC_API_KEY=sk-ant-...
